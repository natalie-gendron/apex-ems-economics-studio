# Root requirements for Streamlit Community Cloud (it reads dependencies from
# the repository root). Pinned to the exact versions the app and its 41-test
# suite were verified against, so the cloud build matches the tested build.
streamlit==1.50.0
pandas==2.3.3
numpy>=2.0,<3
plotly==6.9.0
pydantic==2.13.4
openpyxl==3.1.5
pytest>=8.0
