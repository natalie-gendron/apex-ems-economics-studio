# Root requirements for Streamlit Community Cloud (it reads dependencies from
# the repository root). Verified on pandas 2.3.3 and 3.0.5; ranges stay loose
# enough to resolve wheels on Cloud's Python 3.14.
streamlit>=1.50
pandas>=2.3,<4
numpy>=2.0,<3
plotly>=6.0,<7
pydantic>=2.5,<3
openpyxl>=3.1,<4
pytest>=8.0,<9
